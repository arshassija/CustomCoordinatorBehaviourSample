<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.arshdeep.sweetanims">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.arshdeep.sweetanims.dashboard.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />

                <category android:name="android.intent.category.BROWSABLE" />

<!--
                <data
                    android:host="open"
                    android:scheme="m91" />
-->
                <!--<data
                    android:host="money91.app.link"
                    android:scheme="https" />-->
                <data
                    android:host="sweetanims.page.link"
                    android:scheme="https" />
            </intent-filter>
        </activity>
        <activity
            android:name=".coordinator_bahviour.CustomCoordinatorLayoutBehaviourActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".circular_reveal.CircularRevealActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".animate_changes.AnimateLayoutChangesActivity"
            android:screenOrientation="portrait" />

        <receiver
            android:name=".InstallReferrerReceiver"
            android:exported="true"
            android:permission="android.permission.INSTALL_PACKAGES">
            <intent-filter>
                <action android:name="com.android.vending.INSTALL_REFERRER" />
            </intent-filter>
        </receiver>

    </application>

</manifest>